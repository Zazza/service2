# Service 2 - Server app

```
# composer install
# vendor/bin/doctrine-migrations --configuration=migration-config.php --db-configuration=migration-dbconfig.php migrations:migrate --no-interaction
```
